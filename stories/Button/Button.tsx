import React from 'react';
import MuiButton from '@mui/material/Button';

const Button: React.FC = (props: any) => {
  return <MuiButton variant='contained'>Hello</MuiButton>;
};

export default Button;
