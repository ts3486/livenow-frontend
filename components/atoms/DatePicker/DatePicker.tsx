import React from 'react';
import MuiTextField from '@material-ui/core/TextField';

const TextField = (props: any) => {
  return <MuiTextField label='Filled' variant='filled' />;
};

export default TextField;
